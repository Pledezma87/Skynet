/* import express from "express";
import { login, register, refreshToken } from "../controllers/SecurityController.js";

const securityRouter = express.Router();

// Ruta para el inicio de sesión (login)
securityRouter.post("/login", login);

// Ruta para el registro de usuarios (register)
securityRouter.post("/register", register);

// Ruta para la renovación de tokens (refresh token)
securityRouter.post("/refresh-token", refreshToken);

export default securityRouter;
 */